CREATE TABLE Products (
  Product VARCHAR(50),
  Price DECIMAL,
  Category VARCHAR(50),
  Manufacturer VARCHAR(50)
);

CREATE TABLE Purchases (
  Buyer VARCHAR(50),
	Seller VARCHAR(50),
	Product VARCHAR(50)
);

